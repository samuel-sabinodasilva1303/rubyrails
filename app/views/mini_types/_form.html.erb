<%= form_for(mini_type) do |f| %>
  <% if mini_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mini_type.errors.count, "error") %> prohibited this mini_type from being saved:</h2>

      <ul>
      <% mini_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :acromyn %>
    <%= f.text_field :acromyn %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
